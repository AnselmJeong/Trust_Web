
   
# for public goods, for single round
   transaction = {
       "user_id":
	"user_email":
	"game_name": "public goods game"
       "round": round_num,
       "timestamp": datetime.datetime.now().isoformat(),
       "human_contribution": human_contribution
	"computer_contributions": [computer_contributions]
       "human_payoff": human_payoff
   }
   
	
	# for section 1, for single round
    transaction = {
        "user_id":
		"user_email":
		"game_name": "trust game"
		"section_num": 1,
        "round": round_num,
        "timestamp": datetime.datetime.now().isoformat(),
        "amount_sent": amount_sent,
        "amount_returned": amount_to_return,
        "human_profit": amount_sent - amount_to_return,
        "player_b_profit": amount_to_return - (amount_sent * PROLIFERATION_FACTOR),
    }
	
   
   # for section 2 for single round
    transaction = {
        "user_id":
		"user_email":
		"game_name": "trust game"
		"section_num": section_num
		"stage_num": 2,
        "round": round_num,
        "timestamp": datetime.datetime.now().isoformat(),
        "player_b_profile": player_b_profile,
        "amount_sent": amount_sent,
        "amount_returned": amount_to_return,
        "message": message,
        "human_profit": amount_returned - amount_sent,
        "player_b_profit": (amount_sent * PROLIFERATION_FACTOR) - amount_to_return,
    }
	
	